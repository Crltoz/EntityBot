name: Trigger auto deployment for entitybot

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/entitybot-AutoDeployTrigger-b075b73e-de37-4bc8-8ac2-7f9aa72ff2de.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.ENTITYBOT_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.ENTITYBOT_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.ENTITYBOT_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          _targetLabelKey_: _targetLabel_
          registryUrl: 
          registryUsername: ${{ secrets.ENTITYBOT_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.ENTITYBOT_REGISTRY_PASSWORD }}
          containerAppName: entitybot
          resourceGroup: DefaultResourceGroup-EUS2
          imageToBuild: default/[parameters('containerAppName')]:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


